# ComplyMail SMTP Proxy configuration.

listen_addr: ":2525"

upstream:
  host: "mail.example.com"
  port: 25

backend:
  api_url: "http://api:8080"
  timeout_seconds: 5

review:
  mailbox: "review@example.com"

metrics_addr: ":2526"
